# Mermaid Visual Editor - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品定位
一个基于 Web 的 Mermaid 图表可视化编辑器，提供实时预览、智能编辑、多图表类型支持、模板库和导出功能。

### 1.2 目标用户
- 开发者：编写技术文档、系统架构图
- 产品经理：绘制业务流程图、用户旅程图
- 项目经理：创建项目甘特图、时间线
- 教育工作者：制作教学图表、知识结构图

### 1.3 核心价值
- **零学习成本**：通过模板快速上手
- **实时反馈**：所见即所得的编辑体验
- **专业输出**：支持多种导出格式
- **高效编辑**：智能代码补全和语法高亮

---

## 2. 功能需求

### 2.1 功能一：实时预览系统 ⚡

#### 2.1.1 功能描述
用户在代码编辑器中输入 Mermaid 代码时，右侧预览区域实时渲染对应的图表，提供即时反馈。

#### 2.1.2 用户故事
- **作为** 一个用户
- **我希望** 在编辑代码时能实时看到图表效果
- **以便于** 快速验证代码正确性和调整图表样式

#### 2.1.3 功能需求

**FR-1.1 实时渲染**
- 用户在代码编辑器中输入或修改代码时，预览区域自动更新
- 更新延迟 < 300ms（防抖处理）
- 支持大代码量（>1000 行）的流畅渲染

**FR-1.2 错误处理**
- 当代码语法错误时，预览区域显示错误信息
- 错误信息包含：错误类型、错误位置（行号）、错误描述
- 错误信息样式清晰，易于理解

**FR-1.3 加载状态**
- 渲染过程中显示加载动画
- 渲染完成后平滑过渡到图表显示

**FR-1.4 布局适配**
- 支持左右分栏布局（代码 | 预览）
- 支持上下分栏布局（代码在上，预览在下）
- 支持全屏预览模式
- 响应式设计，适配不同屏幕尺寸

**FR-1.5 预览交互**
- 支持图表缩放（放大/缩小）
- 支持图表拖拽移动
- 支持重置视图（回到初始位置和缩放）

#### 2.1.4 非功能需求
- 性能：渲染延迟 < 300ms
- 兼容性：支持 Chrome、Firefox、Safari、Edge 最新版本
- 可访问性：支持键盘导航

#### 2.1.5 验收标准
- ✅ 代码修改后 300ms 内图表更新
- ✅ 语法错误时显示清晰的错误信息
- ✅ 支持至少 2000 行代码的流畅渲染
- ✅ 支持三种布局模式切换
- ✅ 预览区域支持缩放和拖拽

---

### 2.2 功能二：智能代码编辑 ✨

#### 2.2.1 功能描述
提供专业的代码编辑器，包含语法高亮、自动补全、代码格式化、语法检查等功能，提升编辑效率。

#### 2.2.2 用户故事
- **作为** 一个用户
- **我希望** 编辑器能帮助我快速编写正确的 Mermaid 代码
- **以便于** 提高编辑效率，减少语法错误

#### 2.2.3 功能需求

**FR-2.1 语法高亮**
- 支持 Mermaid 语法高亮
- 关键字、字符串、注释、变量等不同颜色区分
- 支持深色/浅色主题切换
- 高亮样式可配置

**FR-2.2 自动补全**
- 输入时自动提示 Mermaid 关键字
- 提示常用图表类型（flowchart, sequenceDiagram 等）
- 提示图表节点类型（rectangle, circle, diamond 等）
- 提示连接线类型（--> , --- 等）
- 支持 Tab 键快速补全

**FR-2.3 代码格式化**
- 一键格式化代码（快捷键：Shift + Alt + F）
- 自动缩进和对齐
- 保持代码可读性

**FR-2.4 语法检查**
- 实时检测语法错误
- 错误位置标记（红色波浪线）
- 错误提示悬浮框（hover 显示详细信息）
- 警告信息标记（黄色波浪线）

**FR-2.5 代码折叠**
- 支持代码块折叠/展开
- 支持按图表类型折叠
- 折叠状态记忆

**FR-2.6 行号显示**
- 显示行号
- 点击行号可快速定位
- 错误行号高亮显示

**FR-2.7 搜索替换**
- 支持代码搜索（Ctrl/Cmd + F）
- 支持正则表达式搜索
- 支持批量替换（Ctrl/Cmd + H）
- 搜索结果高亮显示

**FR-2.8 多光标编辑**
- 支持多光标同时编辑
- 支持列选择模式
- 支持批量编辑

#### 2.2.4 非功能需求
- 性能：代码补全响应时间 < 100ms
- 兼容性：支持主流浏览器
- 可访问性：支持键盘快捷键

#### 2.2.5 验收标准
- ✅ 语法高亮正确显示所有 Mermaid 语法元素
- ✅ 自动补全覆盖所有常用关键字和图表类型
- ✅ 代码格式化后保持可读性
- ✅ 语法错误实时检测并标记
- ✅ 支持所有常用编辑器快捷键

---

### 2.3 功能三：多图表类型支持 📊

#### 2.3.1 功能描述
支持 Mermaid 的所有图表类型，包括流程图、序列图、类图、状态图、ER 图、甘特图、思维导图等。

#### 2.3.2 用户故事
- **作为** 一个用户
- **我希望** 能创建各种类型的专业图表
- **以便于** 满足不同场景的可视化需求

#### 2.3.3 功能需求

**FR-3.1 支持的图表类型**

| 图表类型 | 英文名称 | 用途 | 优先级 |
|---------|---------|------|--------|
| 流程图 | Flowchart | 业务流程、算法流程 | P0 |
| 序列图 | Sequence Diagram | 系统交互、API 调用 | P0 |
| 类图 | Class Diagram | 面向对象设计、系统架构 | P0 |
| 状态图 | State Diagram | 状态机、工作流 | P0 |
| ER 图 | Entity Relationship Diagram | 数据库设计 | P1 |
| 甘特图 | Gantt Chart | 项目管理、时间线 | P1 |
| 思维导图 | Mindmap | 知识梳理、头脑风暴 | P1 |
| 饼图 | Pie Chart | 数据统计 | P2 |
| Git 图 | Git Graph | 版本控制可视化 | P2 |
| 用户旅程图 | User Journey | 用户体验设计 | P2 |
| C4 图 | C4 Diagram | 系统架构设计 | P2 |

**FR-3.2 图表类型切换**
- 提供图表类型选择器（下拉菜单或标签页）
- 切换时自动生成对应类型的模板代码
- 切换时保留用户已输入的代码（如果兼容）

**FR-3.3 图表配置**
- 每个图表类型支持特定配置选项
- 配置面板显示当前图表类型可用的配置
- 配置修改实时生效

**FR-3.4 图表验证**
- 检测代码中的图表类型
- 如果代码包含多种图表类型，提示用户
- 支持多图表类型混合显示（如果 Mermaid 支持）

**FR-3.5 图表预览优化**
- 不同图表类型使用不同的默认样式
- 支持图表主题切换
- 支持自定义图表样式

#### 2.3.4 非功能需求
- 兼容性：支持 Mermaid.js 最新版本的所有图表类型
- 性能：图表渲染时间 < 500ms
- 可扩展性：易于添加新的图表类型支持

#### 2.3.5 验收标准
- ✅ 支持至少 8 种核心图表类型（P0 + P1）
- ✅ 图表类型切换流畅，无卡顿
- ✅ 每种图表类型都有对应的模板代码
- ✅ 图表渲染正确，样式美观
- ✅ 支持图表配置实时生效

---

### 2.4 功能四：模板库系统 📚

#### 2.4.1 功能描述
提供丰富的图表模板库，用户可以选择模板快速创建图表，减少从零开始绘制的时间。

#### 2.4.2 用户故事
- **作为** 一个用户
- **我希望** 能快速找到适合的图表模板
- **以便于** 快速开始项目，学习最佳实践

#### 2.4.3 功能需求

**FR-4.1 模板分类**
- 按图表类型分类（流程图、序列图等）
- 按使用场景分类（系统架构、业务流程、项目管理等）
- 按复杂度分类（简单、中等、复杂）
- 支持多标签分类

**FR-4.2 模板展示**
- 模板列表展示（网格布局或列表布局）
- 每个模板显示：缩略图、名称、描述、标签
- 支持模板搜索（按名称、描述、标签）
- 支持模板筛选（按分类、标签）

**FR-4.3 模板预览**
- 点击模板可预览完整图表
- 预览窗口显示模板代码
- 支持在预览窗口直接编辑

**FR-4.4 模板应用**
- 点击"使用模板"按钮应用模板
- 应用后代码编辑器显示模板代码
- 支持模板代码的二次编辑

**FR-4.5 模板管理**
- 用户可收藏常用模板
- 用户可创建自定义模板
- 用户可分享自定义模板（可选功能）

**FR-4.6 内置模板库**

**流程图模板：**
- 基础流程图
- 决策流程图
- 循环流程图
- 分支流程图

**序列图模板：**
- 用户登录流程
- API 调用流程
- 微服务交互
- 数据库操作流程

**类图模板：**
- MVC 架构
- 三层架构
- 领域模型
- 数据库模型

**状态图模板：**
- 订单状态流转
- 用户状态机
- 工作流状态
- 生命周期管理

**ER 图模板：**
- 用户系统 ER 图
- 电商系统 ER 图
- 博客系统 ER 图

**甘特图模板：**
- 项目计划甘特图
- 任务时间线
- 里程碑规划

**思维导图模板：**
- 知识结构图
- 头脑风暴模板
- 学习计划图

#### 2.4.4 非功能需求
- 性能：模板加载时间 < 200ms
- 可扩展性：易于添加新模板
- 数据量：支持至少 50 个内置模板

#### 2.4.5 验收标准
- ✅ 提供至少 30 个内置模板
- ✅ 模板分类清晰，易于查找
- ✅ 支持模板搜索和筛选
- ✅ 模板预览流畅，代码可编辑
- ✅ 模板应用后代码正确显示

---

### 2.5 功能五：导出功能 💾

#### 2.5.1 功能描述
支持将图表导出为多种格式，包括 PNG、SVG、PDF 等，方便用户在其他平台或文档中使用。

#### 2.5.2 用户故事
- **作为** 一个用户
- **我希望** 能将图表导出为不同格式
- **以便于** 在文档、演示文稿或其他工具中使用

#### 2.5.3 功能需求

**FR-5.1 PNG 导出**
- 支持导出为 PNG 格式
- 可配置分辨率（1x, 2x, 3x）
- 可配置背景色（透明/白色/自定义）
- 可配置图片尺寸（自定义宽高）
- 导出文件名可自定义

**FR-5.2 SVG 导出**
- 支持导出为 SVG 格式
- SVG 代码可直接复制
- SVG 文件可下载
- 支持 SVG 优化（去除冗余代码）

**FR-5.3 PDF 导出**
- 支持导出为 PDF 格式
- 可配置页面大小（A4, Letter 等）
- 可配置页面方向（横向/纵向）
- 可配置页边距
- 支持多页 PDF（如果图表较大）

**FR-5.4 代码导出**
- 导出 Mermaid 代码（.mmd 文件）
- 导出为 Markdown 格式（包含代码块）
- 导出为 HTML 格式（可独立运行）
- 代码格式化后导出

**FR-5.5 导出预览**
- 导出前可预览导出效果
- 预览窗口显示导出参数
- 支持调整导出参数

**FR-5.6 批量导出**
- 支持同时导出多种格式
- 支持批量导出多个图表（如果支持多图表）

**FR-5.7 导出历史**
- 记录导出历史（可选功能）
- 快速重新导出

#### 2.5.4 非功能需求
- 性能：PNG 导出时间 < 2s（1000x1000 像素）
- 质量：PNG 导出质量清晰，无模糊
- 兼容性：导出的文件可在主流软件中打开

#### 2.5.5 验收标准
- ✅ 支持 PNG、SVG、PDF 三种图片格式导出
- ✅ 支持代码格式导出（Mermaid、Markdown、HTML）
- ✅ PNG 导出质量清晰，支持高分辨率
- ✅ SVG 导出代码正确，可在浏览器中正常显示
- ✅ PDF 导出格式正确，可在 PDF 阅读器中打开
- ✅ 导出文件名可自定义
- ✅ 导出操作流畅，无卡顿

---

## 3. 用户体验需求

### 3.1 界面设计
- 简洁现代的界面设计
- 清晰的视觉层次
- 一致的设计语言
- 支持深色/浅色主题

### 3.2 交互设计
- 直观的操作流程
- 清晰的反馈提示
- 流畅的动画过渡
- 友好的错误提示

### 3.3 响应式设计
- 支持桌面端（1920x1080 及以上）
- 支持平板端（768x1024）
- 支持移动端（375x667 及以上，可选）

---

## 4. 技术约束

### 4.1 浏览器兼容性
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

### 4.2 性能要求
- 首屏加载时间 < 2s
- 代码编辑响应时间 < 100ms
- 图表渲染时间 < 500ms
- 导出操作时间 < 3s

### 4.3 安全要求
- 代码内容不发送到服务器（本地处理）
- 导出文件安全，无恶意代码
- 支持 HTTPS

---

## 5. 优先级规划

### Phase 1 (MVP)
- ✅ 实时预览系统（基础版）
- ✅ 智能代码编辑（基础版）
- ✅ 多图表类型支持（核心 4 种）
- ✅ 模板库系统（基础模板）
- ✅ 导出功能（PNG、SVG）

### Phase 2
- 实时预览系统（增强版）
- 智能代码编辑（增强版）
- 多图表类型支持（全部类型）
- 模板库系统（丰富模板）
- 导出功能（PDF、代码导出）

### Phase 3
- 云端同步
- 分享功能
- 协作编辑
- 用户系统

---

## 6. 成功指标

### 6.1 功能指标
- 图表渲染成功率 > 99%
- 导出成功率 > 99%
- 模板使用率 > 60%

### 6.2 性能指标
- 首屏加载时间 < 2s
- 代码编辑响应时间 < 100ms
- 图表渲染时间 < 500ms

### 6.3 用户体验指标
- 用户满意度 > 4.5/5
- 错误率 < 1%
- 用户留存率 > 40%

